# API base URL (no trailing slash). Used when calling the resume backend.
# Default if unset: http://localhost:8080
# With Docker Compose (portal on 8081): http://localhost:8081
VITE_API_URL=http://localhost:8080
