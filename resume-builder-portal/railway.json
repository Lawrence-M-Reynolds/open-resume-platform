{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "./../gradlew :resume-builder-portal:build -x test"
  },
  "deploy": {
    "startCommand": "java -Xmx512m -jar build/libs/resume-builder-portal-0.0.1-SNAPSHOT.jar",
    "healthcheckPath": "/actuator/health",
    "restartPolicyType": "ON_FAILURE"
  }
}