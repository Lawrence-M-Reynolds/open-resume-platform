{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "./gradlew :resume-builder-portal:bootJar -x test"
  },
  "deploy": {
    "startCommand": "java -Xmx512m -jar document-generator-gateway/build/libs/app.jar",
    "healthcheckPath": "/actuator/health",
    "restartPolicyType": "ON_FAILURE"
  }
}